apiVersion: v1
kind: Pod
metadata:
  name: julia
  labels:
    name: julia
spec:
  containers:
  - name: julia
    image: us-central1-docker.pkg.dev/project-lee-1/docker/julia:1.7.3
    resources:
      limits:
        memory: "16G"
        cpu: "4"
    ports:
      - containerPort: 1234
  serviceAccountName: admin
  restartPolicy: Never
